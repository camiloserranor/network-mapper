# Binaries
*.exe
*.exe~
/network-mapper

# Collected data (generated, not committed)
topology-collected.json
topology.json
topology.validation*.json
topology-cml.json

# Test/local config (may contain credentials)
config.*test*.yaml
**/config.*test*.yaml
examples/config.sonic.yaml

# Real deployment data (contains internal hostnames/IPs)
examples/sample-deployment.json

# TLS certificate cache
.certs/

# Raw gNMI data dumps (diagnostic, may contain sensitive data)
gnmi-raw-data/

# Runtime data directory (snapshots + logs, generated at runtime)
data/

# IDE / OS
.vs/
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
